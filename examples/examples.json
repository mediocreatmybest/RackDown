{
  "$schema-note": "Canonical examples are every *.rackdown file in this directory; they are discovered automatically and checked/rendered by CI. The list below declares which renders are additionally committed as SVG under generatedDirectory. Add an entry here (and run pnpm examples:generate) when a curated README/example render becomes canonical.",
  "generatedDirectory": "generated",
  "defaultRenderOptions": ["--sizing", "responsive"],
  "generated": []
}
