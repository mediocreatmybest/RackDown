{
  "$schema-note": "Canonical examples are every *.rackdown file in this directory; they are discovered automatically and checked/rendered by CI. The list below declares which renders are additionally committed as SVG under generatedDirectory.",
  "generatedDirectory": "generated",
  "defaultRenderOptions": ["--sizing", "responsive"],
  "generated": [
    {
      "source": "home-lab.rackdown",
      "output": "home-lab.svg"
    },
    {
      "source": "routing.rackdown",
      "output": "routing.svg"
    },
    {
      "source": "shared-rows.rackdown",
      "output": "shared-rows.svg"
    }
  ]
}
